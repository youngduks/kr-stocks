# 요약 API를 제공하는 Next.js 서버 주소.
# 로컬 개발 시에는 PC의 LAN IP를 쓴다 (실기기에서 localhost는 폰 자신을 가리킨다).
#   예: EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:3000
EXPO_PUBLIC_API_BASE_URL=https://kr-stocks.com

# 서버에 VOICE_API_SECRET을 설정했다면 같은 값을 넣는다. 안 썼으면 비워 둔다.
EXPO_PUBLIC_VOICE_API_KEY=
